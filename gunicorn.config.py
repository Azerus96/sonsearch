# Основные настройки
bind = "0.0.0.0:10000"  # Адрес и порт для привязки
workers = 2  # Количество процессов-работников (обычно 2-4 для небольших приложений)
threads = 4  # Количество потоков на каждого работника
worker_class = 'gthread'  # Использование потоков для асинхронной обработки

# Таймауты
timeout = 120  # Максимальное время выполнения запроса
graceful_timeout = 30  # Время на завершение текущих запросов при перезапуске
keep_alive = 5  # Время поддержания соединения между запросами

# Ограничения ресурсов
max_requests = 1000  # Максимальное количество запросов до перезапуска работника
max_requests_jitter = 50  # Случайное отклонение для max_requests
worker_connections = 1000  # Максимальное количество одновременных соединений

# Логирование
accesslog = '-'  # Логи доступа (- означает stdout)
errorlog = '-'  # Логи ошибок
loglevel = 'info'  # Уровень логирования
access_log_format = '%({x-forwarded-for}i)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s "%(f)s" "%(a)s"'

# Безопасность
limit_request_line = 4094  # Максимальная длина строки запроса
limit_request_fields = 100  # Максимальное количество заголовков
limit_request_field_size = 8190  # Максимальный размер заголовка

# Настройки процесса
daemon = False  # Запуск в фоновом режиме
pidfile = None  # Файл с PID процесса
user = None  # Пользователь для запуска
group = None  # Группа для запуска
umask = 0  # Права доступа к создаваемым файлам

# Настройки отладки
reload = False  # Автоматическая перезагрузка при изменении кода
reload_engine = 'auto'  # Механизм отслеживания изменений
spew = False  # Детальное логирование

# Настройки SSL (если нужно)
# keyfile = '/path/to/keyfile'
# certfile = '/path/to/certfile'
# ssl_version = 'TLS'
